services:
  - type: web
    name: weather-advisory-bot
    runtime: python
    runtimeVersion: 3.12.7
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
